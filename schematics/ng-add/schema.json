{
  "$schema": "http://json-schema.org/schema",
  "$id": "AscnNgAddSchema",
  "title": "@ascn ng-add schematic",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to add @ascn to.",
      "$default": {
        "$source": "projectName"
      }
    },
    "skipInstall": {
      "type": "boolean",
      "default": false,
      "description": "Skip installing dependency packages."
    }
  }
}
